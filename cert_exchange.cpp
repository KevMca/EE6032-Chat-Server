// Create certificate
// Send over socket
// Verify certificate

#include "cert.h"

int main(int argc, char* argv[])
{
    /*// 1. Subject Certificate file
    // 2. CA Certificate file
    // 3. Private Key file
    std::string name = "Alice";
    CryptoPP::RSA::PrivateKey privateKey;
    CryptoPP::RSA::PublicKey publicKey;

    std::cout << "Creating keys" << std::endl;
    createKeys(&privateKey, &publicKey, 2048);
    std::cout << "Keys created" << std::endl;

    Certificate certA(name, publicKey);
    if (certA.verify() == 0) {
        std::cout << "Valid" << std::endl;
    }
    else{
        std::cout << "Invalid" << std::endl;
    }
    */
   return 0;
}
